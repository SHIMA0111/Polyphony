# Step 20: Server: Kratos session auth swap + identity migration

## Meta
- **Type**: feature
- **Components**: server
- **Wave**: 3 (parallel group — all steps of the same wave can run concurrently)
- **Depends on**: Step 1: Go server foundation: DI/router split, structured logging, lint, config | Step 2: Go test infrastructure: testcontainers helper + shared mocks | Step 11: Infra: Ory Kratos services in docker-compose
- **Unlocks**: Step 30: Web: Kratos-driven login/registration + auth data-plane flip | Step 33: Server: Redis rate limiting + Kratos session cache
- **Size**: 1 PR (a few hours for one AI agent)

## Context
Phase 9 of `phases.md` requires migrating auth from SimpleJWT to Ory Kratos. Step 11 already brought up a live, locally-runnable Kratos instance (public API on `4433`, admin API on `4434`, identity schema mirroring `users.email`/`users.username`) but touched no application code. This step lands the Go-side half of the swap: a second `domainauth.AuthService` implementation backed by Kratos's self-service and session-verification APIs, selectable via an `AUTH_MODE` environment flag, plus the `users.kratos_identity_id` column and a one-off backfill script that links pre-existing SimpleJWT users to newly-created Kratos identities. Per the plan's explicit auth data-plane sequencing, SimpleJWT remains the `docker-compose.yml` default through this wave — Step 30 owns flipping the compose default and rewriting the web BFF proxy to forward Kratos session cookies instead of Bearer JWTs.

## Goal
After this PR, `server/internal/interface/auth/kratos.go` provides a `KratosAuthService` that implements the existing `domainauth.AuthService` interface (`Register`, `Login`, `ValidateToken`) using Kratos's self-service registration/login API flows and the `/sessions/whoami` endpoint, with the caller's local `users.id` resolved through a new `users.kratos_identity_id` column. `server/internal/app/container.go` (from Step 1) picks `SimpleJWTService` or `KratosAuthService` based on a new `AUTH_MODE` config value (`simple_jwt` default, `kratos` opt-in) without changing any downstream usecase/handler code, since both implementations satisfy the same interface. The auth middleware accepts credentials from either an `Authorization: Bearer` header or a Kratos session cookie, and CORS is configured to allow credentialed cross-origin requests from the web origin. A `cmd/kratosmigrate` one-off tool backfills existing SimpleJWT users into Kratos identities via the Kratos Admin API and populates `kratos_identity_id`. `docker-compose.yml`'s `api` service still runs with `AUTH_MODE=simple_jwt` by default; running it with `AUTH_MODE=kratos` (and Step 11's Kratos services up) exercises the full Kratos-backed path end to end.

## Scope
- [x] `schema.sql`: add `kratos_identity_id UUID NULL` to the `users` table, with a `UNIQUE` constraint (nullable columns allow multiple `NULL`s in Postgres, so this does not block SimpleJWT-only users) — e.g. `CONSTRAINT users_kratos_identity_id_unique UNIQUE (kratos_identity_id)`. Generate the migration via `task migrate:generate -- add_users_kratos_identity_id` from the repo root (do not hand-write the migration file or edit `atlas.sum`).
- [x] `server/internal/domain/user/entity.go`: add `KratosIdentityID *string` (nullable) to `User`.
- [x] `server/internal/domain/user/repository.go`: extend `UserRepository` with `GetByKratosIdentityID(ctx context.Context, kratosIdentityID string) (*User, error)` (returns `domain.ErrNotFound` if unmapped) and `SetKratosIdentityID(ctx context.Context, userID, kratosIdentityID string) error`.
- [x] `server/internal/interface/repository/postgres/user_repository.go`: implement both new methods; update `Create`, `GetByID`, `GetByEmail`, `GetByUsername`'s `SELECT`/scan lists to include `kratos_identity_id`; map the new unique constraint's violation to a new `domain.ErrKratosIdentityAlreadyLinked` (add this sentinel to `server/internal/domain/errors.go`).
- [x] `server/internal/testutil/mocks` (from Step 2): extend `mocks.UserRepo` with the two new methods (map-backed, mirrors the Postgres semantics) so existing and new tests build against it instead of a third hand-rolled fake.
- [x] `server/internal/interface/auth/kratos.go` (new file, sibling to `simple_jwt.go`): `KratosAuthService` implementing `domainauth.AuthService`, constructed via `NewKratosAuthService(userRepo user.UserRepository, publicURL, adminURL, cookieName string, httpClient *http.Client) *KratosAuthService`. Hand-roll the REST calls with `net/http` + `encoding/json` DTOs, following the existing pattern in `server/internal/interface/gateway/llm_client.go` (private DTO structs, a shared timeout-configured `http.Client`) rather than adding the full `ory/client-go` SDK dependency:
  - `Register(ctx, email, username, password)`: `GET {publicURL}/self-service/registration/api` (header `Accept: application/json`) to obtain a flow ID, then `POST {publicURL}/self-service/registration?flow=<id>` with `{"method":"password","password":password,"traits":{"email":email,"username":username}}`. On success, the native/API flow response body contains `identity.id` and a `session_token`. Create the local `users` row (new UUID `id`, `Email`/`Username` from the request, a placeholder unusable `PasswordHash` since Kratos now owns credential verification, `KratosIdentityID` set to `identity.id`) via `userRepo.Create`, then `userRepo.SetKratosIdentityID`. Return `&domainauth.TokenPair{AccessToken: session_token, TokenType: "Bearer"}`. Map a `4xx` response whose Kratos error body indicates an existing identifier conflict (registration UI node validation error) to `domain.ErrEmailAlreadyExists` / `domain.ErrUsernameAlreadyExists` to preserve the existing handler-level error mapping in `auth_handler.go`.
  - `Login(ctx, email, password)`: `GET {publicURL}/self-service/login/api`, then `POST {publicURL}/self-service/login?flow=<id>` with `{"method":"password","identifier":email,"password":password}`. On success, look up the local user via `userRepo.GetByKratosIdentityID(ctx, session.identity.id)`; if `domain.ErrNotFound` (identity exists in Kratos but has no local row yet — e.g. created directly via the Admin API rather than through this service), self-heal by creating the local row from `session.identity.traits` the same way `Register` does. Return `&domainauth.TokenPair{AccessToken: session_token, TokenType: "Bearer"}`. Map Kratos's `400`/incorrect-credentials UI error to `domain.ErrInvalidCredentials`.
  - `ValidateToken(ctx, token string)`: calls `GET {publicURL}/sessions/whoami`. Interpret `token` using this convention (documented in a GoDoc comment on the method, since it is the contract the auth middleware below must honor): if `token` has the prefix `"cookie:"`, strip it and set header `Cookie: {cookieName}=<rest>`; otherwise treat it as an opaque native session token and set header `X-Session-Token: <token>`. On a `200` response, extract `identity.id` from the body and resolve the local user via `userRepo.GetByKratosIdentityID`, returning `&domainauth.Claims{UserID: localUser.ID}`. On any non-200 or lookup failure, return `domain.ErrInvalidToken`.
  - GoDoc every exported symbol per `CLAUDE.md` (what/why/constraints/error behavior), including the `"cookie:"` prefix convention on `ValidateToken` and the self-healing behavior in `Login`.
- [x] `server/internal/interface/middleware/auth.go`: extend `JWTAuth`'s credential extraction (keep the exported function name for compatibility with Step 1's route registrars) so that when the `Authorization` header is absent, it falls back to reading a cookie named by a new parameter (default `ory_kratos_session`, configurable via the constructor) and, if present, calls `tokenValidator.ValidateToken(ctx, "cookie:"+cookie.Value)`; when the `Authorization: Bearer <token>` header is present, keep passing `<token>` unprefixed as today (this covers both SimpleJWT JWTs and Kratos native session tokens returned by `Register`/`Login` above, both of which `KratosAuthService.ValidateToken` treats as `X-Session-Token` values). Update the constructor signature to `JWTAuth(tokenValidator auth.TokenValidator, cookieName string) echo.MiddlewareFunc` (or add a new `JWTAuthWithCookie` variant if preserving the zero-arg constructor for other callers matters more — pick one and update the single call site in `server/internal/app/router.go`/route registrars from Step 1 accordingly). Return HTTP 401 if neither an `Authorization` header nor the named cookie is present.
- [x] `server/internal/app/router.go` (from Step 1): add `AllowCredentials: true` to the existing `echomw.CORSConfig` so the browser will send/receive the Kratos session cookie cross-origin from `http://localhost:3000` to `http://localhost:8080`; confirm `cfg.CORSOrigins` never resolves to `"*"` (already the case — it defaults to `http://localhost:3000` and is env-driven), since credentialed CORS is invalid with a wildcard origin.
- [x] `server/internal/infrastructure/config/config.go`: add `AuthMode string` (env `AUTH_MODE`, default `"simple_jwt"`; the only other accepted value is `"kratos"` — treat any other non-empty value as a `Load()` error), `KratosPublicURL string` (env `KRATOS_PUBLIC_URL`, default `"http://localhost:4433"`, only required when `AuthMode == "kratos"`), `KratosAdminURL string` (env `KRATOS_ADMIN_URL`, default `"http://localhost:4434"`), `KratosCookieName string` (env `KRATOS_COOKIE_NAME`, default `"ory_kratos_session"`).
- [x] `server/internal/app/container.go` (from Step 1): in `NewContainer`, branch on `cfg.AuthMode` to construct either `ifauth.NewSimpleJWTService(userRepo, cfg.JWTSecret)` or `ifauth.NewKratosAuthService(userRepo, cfg.KratosPublicURL, cfg.KratosAdminURL, cfg.KratosCookieName, &http.Client{Timeout: 10 * time.Second})`, assigning the result to the same `AuthService`-typed field consumed by `authusecase.NewAuthUsecase` and by the auth middleware wiring — no other container field or route registrar needs to change.
- [x] `docker-compose.yml`: add `AUTH_MODE: ${AUTH_MODE:-simple_jwt}`, `KRATOS_PUBLIC_URL: ${KRATOS_PUBLIC_URL:-http://kratos:4433}`, `KRATOS_ADMIN_URL: ${KRATOS_ADMIN_URL:-http://kratos:4434}`, `KRATOS_COOKIE_NAME: ${KRATOS_COOKIE_NAME:-ory_kratos_session}` as additive lines to the existing `api` service's `environment:` block (do not reorder or restructure the block; `AUTH_MODE` defaulting to `simple_jwt` keeps the compose default unchanged this wave, matching the plan's auth data-plane sequencing).
- [x] `.env.example`: `KRATOS_PUBLIC_URL`/`KRATOS_ADMIN_URL` already exist (added by Step 6/11); add `AUTH_MODE=simple_jwt` and `KRATOS_COOKIE_NAME=ory_kratos_session` to the same `# === Ory Kratos ===` section, with a one-line comment noting `AUTH_MODE=kratos` requires the Step 11 Kratos services to be running.
- [x] Create `server/cmd/kratosmigrate/main.go`: a one-off CLI that reads `DATABASE_URL` and `KRATOS_ADMIN_URL` from the environment, connects to Postgres, selects all `users` rows where `kratos_identity_id IS NULL`, and for each: calls the Kratos Admin API `POST {KRATOS_ADMIN_URL}/admin/identities` with `{"schema_id":"default","traits":{"email":u.Email,"username":u.Username},"credentials":{"password":{"config":{"hashed_password": "<argon2-encoded users.password_hash re-wrapped for Kratos's expected format, or a Kratos-compatible re-hash>"}}}}` — since Kratos's admin identity-import API accepts a pre-hashed password in a documented format (bcrypt/argon2id-encoded PHC string compatible with Kratos's hasher config), reuse the existing `$argon2id$...` string already stored in `users.password_hash` (`server/internal/interface/auth/simple_jwt.go`'s `hashPassword` encodes it in standard PHC form) as `credentials.password.config.hashed_password` directly — then calls `userRepo.SetKratosIdentityID(ctx, u.ID, createdIdentity.ID)`. Log progress per user via `slog` (migrated count, skipped-already-linked count, per-user failures without aborting the whole batch). GoDoc the `main` package's purpose and usage at the top of the file.
- [x] Add/extend tests:
  - `server/internal/interface/auth/kratos_test.go`: use `net/http/httptest.Server` to stand up a fake Kratos public+admin API (registration/login/whoami handlers returning canned JSON) and a `mocks.UserRepo`; cover `Register` (creates local user + link, returns token), `Login` (existing link found), `Login` (self-heal when link missing), `ValidateToken` (both the `"cookie:"`-prefixed and bare-token code paths), and each documented error mapping (`ErrEmailAlreadyExists`, `ErrInvalidCredentials`, `ErrInvalidToken`).
  - `server/internal/interface/middleware/auth_test.go`: add cases for cookie-based extraction (valid cookie → 200/next called with correct `user_id` in context; missing both header and cookie → 401; cookie present but validator rejects it → 401), alongside the existing Bearer-header cases.
  - `server/internal/infrastructure/config/config_test.go`: default `AuthMode` is `"simple_jwt"` when unset; `AUTH_MODE=kratos` parses; an unrecognized `AUTH_MODE` value returns an error from `Load()`.
  - `server/internal/interface/repository/postgres/user_repository_integration_test.go` (extend, build-tagged `integration`, using Step 2's testcontainers helper): `SetKratosIdentityID`/`GetByKratosIdentityID` round-trip; inserting two users with the same `kratos_identity_id` violates the new unique constraint and maps to `domain.ErrKratosIdentityAlreadyLinked`.
  - `server/cmd/kratosmigrate` — a small unit test using `httptest.Server` for the Admin API and an in-memory-style pool-free path is not practical for a `main`-only package; instead, factor the per-user migration logic into an exported, testable function (e.g. `server/cmd/kratosmigrate/migrate.go`'s `migrateUser(ctx, adminURL, httpClient, userRepo, u *user.User) error`) and unit-test that function directly with `mocks.UserRepo` + `httptest.Server`, keeping `main.go` itself a thin wrapper (DB connection + loop + logging only).

## Out of scope
- Flipping `docker-compose.yml`'s `AUTH_MODE` default to `"kratos"` — Step 30 owns that, together with rewriting the web BFF proxy to forward the Kratos cookie and removing SimpleJWT/Bearer forwarding.
- Any change to `web/` — login/registration pages, the API client, or cookie handling on the frontend are entirely Step 30's scope; this step only makes the server capable of accepting a Kratos session, it does not change how the browser obtains one.
- Kratos self-service browser UI pages (`ui_url` targets configured in Step 11's `kratos.yml`) — those routes don't exist until Step 30/44.
- OIDC/social login (`selfservice.methods.oidc`), Ory Hydra, Dex — Step 44 and Step 55.
- Redis-backed session caching for Kratos `whoami` calls (avoiding a network round-trip per request) — Step 33, which explicitly builds on this step's `KratosAuthService`.
- RBAC/`room.Role` changes — Step 13, developed in parallel this same wave; this step's only middleware touch is `interface/middleware/auth.go` (authentication), never `interface/middleware/rbac.go` (authorization), so the two steps edit disjoint files.
- Rate limiting — Step 33.
- Rewriting `SimpleJWTService` or removing it — it remains the default implementation and is untouched except that it now must also populate `KratosIdentityID: nil` implicitly (its `Register` already only sets the fields it knows about; no code change needed there since the new struct field defaults to its zero value).

## Implementation notes
- **Files to read before editing**: `server/internal/domain/auth/service.go` (the `AuthService` interface this step's new type must satisfy — do not change its signature), `server/internal/interface/auth/simple_jwt.go` (existing sibling implementation and its argon2id PHC-format password hash, e.g. `$argon2id$v=19$m=65536,t=1,p=4$<salt>$<hash>`, reused as-is when backfilling into Kratos), `server/internal/interface/middleware/auth.go` (current Bearer-only extraction to extend), `server/internal/domain/user/entity.go` and `repository.go`, `server/internal/interface/repository/postgres/user_repository.go` (scan/bind pattern and unique-violation-to-domain-error mapping via `pgerrcode.UniqueViolation` + `pgErr.ConstraintName`), `server/internal/interface/gateway/llm_client.go` (the hand-rolled REST client pattern — private DTOs + shared `http.Client` with a timeout — to mirror in `kratos.go`), `server/schema.sql` (the `users` table, lines defining `email`/`username`/`password_hash`), `docs/tasks/step11.md` (the Kratos infra this step consumes: public API `4433`, admin API `4434`, identity schema traits `email`/`username`, the `/self-service/registration/api` flow already exercised there is the same one this step's `Register` automates), `docs/tasks/step1.md` (target shape of `server/internal/app/container.go` and `router.go` this step extends).
- **Kratos identity schema alignment**: Step 11's `ory/kratos/identity.schema.json` defines `traits.email` and `traits.username` — this step's `Register`/`Login`/`kratosmigrate` payloads must use exactly those trait names.
- **`domain.ErrKratosIdentityAlreadyLinked`**: add alongside the existing sentinel errors in `server/internal/domain/errors.go`, following the existing `var (...)` block style with a GoDoc comment; wire it into `handleAuthError`-equivalent mapping in `auth_handler.go` only if a new HTTP-facing path actually returns it (the `kratosmigrate` CLI logs-and-skips on this error rather than surfacing it through any HTTP handler, so no `auth_handler.go` change is strictly required — verify and only touch it if a code path needs it).
- **Interface contract, not a breaking change**: `domainauth.AuthService`'s three methods (`Register`, `Login`, `ValidateToken`) are unchanged in signature; `KratosAuthService` is a pure additional implementation, so `authusecase.AuthUsecase`, `auth_handler.go`, and every existing test against `SimpleJWTService` continue to compile and pass unmodified.
- **`"cookie:"` prefix convention**: this is an internal contract between `middleware/auth.go` and whichever `AuthService` is active — `SimpleJWTService.ValidateToken` never receives a `"cookie:"`-prefixed string in practice (browsers only get an `ory_kratos_session` cookie when `AUTH_MODE=kratos`, since SimpleJWT never sets any cookie), so no change to `simple_jwt.go` is needed, but note this explicitly in `kratos.go`'s GoDoc so a future reader understands why the prefix exists.
- **Conflict notes** (per skeleton): this step touches `server/internal/interface/middleware/auth.go` only; Step 13 (same wave) adds a new, separate `server/internal/interface/middleware/rbac.go` file that is chained *after* the auth middleware in route registration — no line-level overlap. If Step 13 lands first, its `RequireRole` middleware already expects `middleware.GetUserID(c)` to be populated by whatever runs first in the chain, which remains true regardless of which `AuthService` populated it. The `docker-compose.yml` edit here is additive-only within the existing `api:` service block (new `environment:` keys), consistent with the convention Step 1/6/11 established — no other same-wave step edits the `api:` block's existing lines.
- **`schema.sql`/migration convention**: edit `server/schema.sql`'s `users` table additively (only the new `kratos_identity_id` column + its unique constraint), then run `task migrate:generate -- add_users_kratos_identity_id` from the repo root; do not hand-write SQL under `server/migrations/` or edit `server/migrations/atlas.sum` by hand.
- English-only code/comments, GoDoc on every new exported symbol, per `CLAUDE.md`.

## Verification
1. [x] `cd server && go build ./...` — compiles with no errors, including the new `cmd/kratosmigrate` binary.
2. [x] `cd server && go vet ./... && gofmt -l .` — no vet findings; `gofmt -l .` prints no files.
3. [x] `cd server && go test ./...` — all existing and new unit tests pass, including `internal/interface/auth/kratos_test.go` (fake Kratos server via `httptest`), `internal/interface/middleware/auth_test.go` (cookie + Bearer cases), `internal/infrastructure/config/config_test.go` (`AUTH_MODE` parsing), and `cmd/kratosmigrate`'s extracted `migrateUser` unit test.
4. [x] `cd server && go test -tags=integration ./...` (Docker running) — the extended `user_repository_integration_test.go` cases for `kratos_identity_id` pass.
5. [x] From the repo root, run `task migrate:generate -- add_users_kratos_identity_id` and confirm a new file appears under `server/migrations/` with `ALTER TABLE users ADD COLUMN kratos_identity_id uuid NULL` (or equivalent Atlas-generated statement) plus the unique constraint, and `server/migrations/atlas.sum` is updated (not hand-edited).
6. [x] Verified in the Wave 3 integration review against the full compose stack with `AUTH_MODE` defaulting to `simple_jwt`: register returned `201` with a JWT `access_token` (two-dot structure) and `GET /rooms` with that Bearer token returned `200`.
7. [ ] **FAILED** in the Wave 3 integration review (register half). With `AUTH_MODE=kratos`: `POST /auth/register` returns `201` but with an **empty** `access_token` — the real Kratos registration API flow response contains only `identity`/`continue_with` and no `session_token`, because `ory/kratos/kratos.yml` does not enable the `session` after-registration hook (`selfservice.flows.registration.after.password.hooks: [{hook: session}]`), and `KratosAuthService.Register` returns `result.SessionToken` without checking it is non-empty (the `httptest` fake in `kratos_test.go` always returns a `session_token`, masking this). The identity itself IS created (`/admin/identities` includes `kuser@example.com`). The rest of the item passes via login instead: `POST /auth/login` returns `200` with an opaque (zero-dot) Kratos session token, and `GET /rooms` with that Bearer token returns `200`, proving `ValidateToken`'s `X-Session-Token` path end to end. Tracked as a Wave 3 review finding.
8. [x] Cookie path verified in the Wave 3 integration review: browser-style login flow (`GET /self-service/login/browser` + CSRF token + cookie jar) yielded an `ory_kratos_session` cookie; `GET /rooms` with that cookie and `Origin: http://localhost:3000` returned `200` with `Access-Control-Allow-Credentials: true` and `Access-Control-Allow-Origin: http://localhost:3000`.
9. [x] Backfill verified in the Wave 3 integration review: `go run ./cmd/kratosmigrate` (against the compose stack's DB/Kratos admin API) logged one line per migrated user (`total:1 migrated:1 failed:0`), the previously-`NULL` `kratos_identity_id` became non-`NULL`, `/admin/identities` now includes the backfilled email, and — bonus — the backfilled user can log in through Kratos with their original password (the imported argon2id PHC hash authenticates).
10. [x] `docker compose down` — clean shutdown, no errors.

## Completion criteria
- [x] `users.kratos_identity_id` column exists with a unique constraint, added via `task migrate:generate` (no hand-written migration SQL).
- [x] `server/internal/interface/auth/kratos.go` implements `domainauth.AuthService` end to end (`Register`, `Login`, `ValidateToken`) against real Kratos self-service/whoami APIs (verified against a fake `httptest`-based Kratos server; Wave 3 integration review confirmed `Login` and `ValidateToken` — both token and cookie paths — against the real Kratos instance, but found `Register` returns an empty session token against real Kratos due to the missing `session` after-registration hook in `ory/kratos/kratos.yml` — see Verification item 7).
- [x] `server/internal/interface/middleware/auth.go` accepts both a Bearer token and a named session cookie, resolving through the same `TokenValidator` interface.
- [x] CORS is configured with `AllowCredentials: true` against a non-wildcard origin list.
- [x] `AUTH_MODE` env flag selects `SimpleJWTService` (default) vs `KratosAuthService` in `server/internal/app/container.go`, with `docker-compose.yml`'s default unchanged (`simple_jwt`).
- [x] `server/cmd/kratosmigrate` backfills existing SimpleJWT users into Kratos identities and populates `kratos_identity_id`.
- [x] All items in Scope are checked off.
- [ ] All Verification checks pass. (1-5 ran and passed; 6-10 require the full docker compose stack and were left for the post-merge integration review.)
